# def modulo_with_offset(x, period, offset):
#     return x - period * (x - offset).floordiv(period)


# def periodic_extension(spikes, tmin, tmax):
#     """Periodically extend the firing times in spikes between tmin and tmax.
#     spikes is a dataframe with (at least) two columns: time and period.
#     """
#     ...
